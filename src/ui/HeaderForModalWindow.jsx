import styled from "styled-components";

const HeaderForModalWindow = styled.div`
  background-color: var(--color-dark);
  color: var(--color-light-3);
  padding: 2rem;
  text-align: center;
  text-transform: uppercase;
  font-size: 2rem;
  font-weight: bold;
  letter-spacing: 2px;
`;

export default HeaderForModalWindow;
